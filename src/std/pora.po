namespace std {
    static void pora_open() {
        initializeMemory();
        startController();
        native_socket_start();
    }
    static void pora_close() {
        shutdownController();
        native_socket_stop();
    }
}
