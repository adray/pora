import std;

namespace Example
{
    struct vec3
    {
        f64 x;
        f64 y;
        f64 z;
    }

    struct transform
    {
        vec3 pos;
        vec3 scale;
    }

    static void main()
    {
        transform t;
        //vec3 pos = t.pos;
        t.scale.z = 10.5;
        f64 x = t.scale.z;
    }
}
