import std;

namespace Test {
    static void main() {
        clock c;
        c.addMinutes((u16)10);
        c.addSeconds((u16)8);
        c.addHours((u16)7);
        
        clock_time time;
        c.getTime(&time);
        println((u8*)time.text, (u32)9);
    }
}
