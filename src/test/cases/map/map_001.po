import std;
namespace Test {
    static void main() {
        map<i64, i64, hash_int64> map;
        map.insert(100, 2);
        map.insert(77, 4);
        
        i64 val;
        boolean found = map.find(100, &val);
        if (found) {
            print_64(val);
        }
    }
}
