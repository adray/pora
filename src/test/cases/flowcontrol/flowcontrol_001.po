import std;

namespace Example
{
    static void main()
    {
        u8* str = "";
    
        i64 i = 0;
        i64 x = 0;
        while ( i < 100 )
        {
            x = x + 2;
            i = i + 1;
            
            print_64(x);
            println(str, (u32)1);
            print_64(i);
            println(str, (u32)1);
        }        
    }
}
