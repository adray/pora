import std;

namespace Test {
    struct myProcess {
        u8* name;
        u32 len;
        u32 pid;
    }
    
    static void main() {
        myProcess[4] procs;
        procs[0].name = "Process Name1";
        procs[0].pid = (u32)123;
        procs[1].name = "Process Name2";
        procs[1].pid = (u32)456;
        
        myProcess x = procs[0];
        myProcess y = procs[1];
        
        println(x.name, (u32)14);
        print_64((i64)x.pid);
        println(y.name, (u32)14);
        print_64((i64)y.pid);
    }
}
